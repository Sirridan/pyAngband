#Utils.py
#utility functions

def parse_dice(dice):
    #returns an xdy string as a tuple (x, y)
    return (0, 0)
    
def roll_dice(dice):
    #does a xdy+z roll of dice
    #may expand to do things like xdy + z + mdn + qdrds
    #qdrds is rolling a ds qdr times
    return 0
    
def one_in(n):
    #returns true if the random result is n
    #so one_in(5) returns true on a 5, false on 1-4
    return True
    